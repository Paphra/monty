#include "monty.h"

/**
 * stack_push - push an integer to the stack
 * @stack: the stack to push
 * @line_number: the line number from the file
 * Return: nothing
 */
void stack_push(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}

/**
 * stack_pop - push an integer to the stack
 * @stack: the stack to push
 * @line_number: the line number from the file
 * Return: nothing
 */
void stack_pop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}

/**
 * stack_pall - push an integer to the stack
 * @stack: the stack to push
 * @line_number: the line number from the file
 * Return: nothing
 */
void stack_pall(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}

/**
 * stack_pint - push an integer to the stack
 * @stack: the stack to push
 * @line_number: the line number from the file
 * Return: nothing
 */
void stack_pint(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
